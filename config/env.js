// 请求地址
const baseUrl = "/admin"

// 阿里字体图标库 https://at.alicdn.com/t/**.css
const iconfontUrl = ``

// 程序配置参数
const app = {
	name: 'cool-admin',

	conf: {
		// 是否显示一级菜单栏
		showAMenu: false,
		// 是否显示路由导航栏
		showRouteNav: true,
		// 是否显示页面进程栏
		showProcess: true,
	}
}

export { baseUrl, iconfontUrl, app }
